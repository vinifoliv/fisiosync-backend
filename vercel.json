{
    "version": 2,
    "builds": [
      {
        "src": "src/main.ts",
        "use": "@vercel/node",
        "config": {
          "installCommand": "npm install && npx prisma migrate deploy && npx prisma generate",
          "buildCommand": "npm run build",
          "includeFiles": [
            "tsconfig.json"
          ]
        }
      }
    ],
    "routes": [
      {
        "src": "/(.)",
        "dest": "src/main.ts",
        "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
        "headers": {
          "Access-Control-Allow-Origin": "",
          "Access-Control-Allow-Headers": "Origin, X-Requested-With, Content-Type, Accept, Authorization",
          "Access-Control-Allow-Methods": "GET, POST, PUT, DELETE, PATCH, OPTIONS"
        }
      }
    ],
    "env": {
      "NODE_ENV": "production",
      "DATABASE_URL": "mysql://sql5749858:3zqv35iHx9@sql5.freesqldatabase.com:3306/sql5749858",
      "JWT_SECRET_KEY": "@AmordaMinhavidacomeçacom4"
    }
  }